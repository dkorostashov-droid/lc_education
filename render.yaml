services:
  - type: web
    name: promodocs
    env: docker
    plan: starter
    autoDeploy: true
    dockerfilePath: ./Dockerfile
    branch: main
    healthCheckPath: /health
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: LLM_PROVIDER
        value: openai
      - key: LLM_API_KEY
        sync: false
      - key: FILES_BASE
        value: "https://promodocs.onrender.com/files"
      - key: API_BASE
        value: "https://promodocs.onrender.com"
      - key: ADMIN_CHAT_ID
        value: "0"
      - key: ALLOWED_CHATS
        value: "[]"
    disk:
      name: data
      mountPath: /app/data
      sizeGB: 2
